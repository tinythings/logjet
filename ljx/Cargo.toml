[package]
name = "ljx"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"

[dependencies]
clap = { version = "4.5", features = ["derive", "wrap_help"] }
colored = "3"
logjet = { path = ".." }
regex = "1.12"
